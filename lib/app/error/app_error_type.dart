
enum AppErrorType {
  unknownApiError,
  canceledError,
  connectionTimeOutError,
  defaultError,
  receivedTimeOutError,
  sendTimeoutCode,
  handshakeError,
  responseError,
  unknownHandlingError,

//Enums based on what we receive from API

//Enums based on internal handling
}